[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "virea-model-motioncraft-smplx-cpu-runtime"
version = "0.2.0"
description = "Portable CPU environment for the MotionCraft VIREA Worker"
readme = "README.md"
requires-python = ">=3.11,<3.12"
authors = [{ name = "Moonweave AI" }]
dependencies = [
  "virea-model-motioncraft-smplx-runtime==0.2.0",
  "numpy==1.26.4; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "torch==2.11.0; sys_platform != 'darwin' or platform_machine != 'x86_64'",
  "torch==2.2.2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "torchvision==0.26.0; sys_platform != 'darwin' or platform_machine != 'x86_64'",
  "torchvision==0.17.2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
]

[project.scripts]
virea-model-motioncraft-smplx-cpu-worker = "virea_motioncraft.worker:main"

[tool.uv.sources]
virea-model-motioncraft-smplx-runtime = { path = "../runtime", editable = false }
torch = [{ index = "pytorch-cpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" }]
torchvision = [{ index = "pytorch-cpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" }]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.setuptools.packages.find]
where = ["src"]
