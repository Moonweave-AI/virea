[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "virea-model-hy-motion-1-cpu-runtime"
version = "0.2.0"
description = "Portable CPU environment for the HY-Motion 1.0 VIREA Worker"
readme = "README.md"
requires-python = ">=3.11,<3.12"
authors = [{ name = "Moonweave AI" }]
dependencies = [
  "virea-model-hy-motion-1-runtime==0.2.0",
  "numpy==1.26.4; sys_platform == 'darwin' and platform_machine == 'x86_64'",
  "torch==2.11.0; sys_platform != 'darwin' or platform_machine != 'x86_64'",
  "torch==2.2.2; sys_platform == 'darwin' and platform_machine == 'x86_64'",
]

[project.scripts]
virea-model-hy-motion-1-cpu-worker = "virea_hy_motion.worker:main"

[tool.uv.sources]
virea-model-hy-motion-1-runtime = { path = "../runtime", editable = false }
torch = [{ index = "pytorch-cpu", marker = "sys_platform == 'linux' or sys_platform == 'win32'" }]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.setuptools.packages.find]
where = ["src"]
