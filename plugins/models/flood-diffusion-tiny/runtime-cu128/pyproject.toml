[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "virea-model-flood-diffusion-tiny-cu128-runtime"
version = "0.1.3"
description = "CUDA 12.8 environment for the shared VIREA Flood Worker"
readme = "README.md"
requires-python = ">=3.11,<3.12"
license = { text = "MIT" }
authors = [{ name = "Moonweave AI" }]
dependencies = [
  "virea-model-flood-diffusion-tiny-runtime==0.1.3",
  "torch==2.11.0",
]

[project.scripts]
virea-model-flood-diffusion-tiny-cu128-worker = "virea_flood.worker:main"

[tool.uv.sources]
virea-model-flood-diffusion-tiny-runtime = { path = "../runtime", editable = false }
torch = { index = "pytorch-cu128" }

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.setuptools.packages.find]
where = ["src"]
